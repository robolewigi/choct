//console key 
//no scroll
//update moouse to stay at y while x is length varable
//enter removes string places it last line if easy
consolecommands function {find run hotkeys goto replace save open}
//make selection from line to line
//multiple selection y

//!full
import java.nio.file.*;
import java.nio.charset.StandardCharsets;
import java.io.*;
import java.util.*;
import java.util.stream.*;

public class main {
static class tab {
 List<StringBuilder> lines = new ArrayList<>(Arrays.asList(new StringBuilder("")));
 int column = 0;
 int line= 0;
 int row = 0;
 int columnSave=0;
 int yScroll= 0;int remaining=0;
 String fileName="";
 String directory="";
 boolean conEnabled=false;
 String conString="";
 tab(String fi) {
  this.fileName= fi;
 }
}

static int width = 80; static int height = 20;
static tab current; static int focused = 0;
static List<tab> tabs = new ArrayList<>();
static String[] previousLines = new String[44];
static {Arrays.fill(previousLines, "");}
static int tick=0; static int[] displayLines=new int[44];
static int[] hotkeyByte= {14,19, 15, 24, 23, 7, 4, 17, 5, 14, 15, 2};
String[] hotkeyName= {"new","save","open","cut", "close", "goto", "run", "leftTab", "rightTab", "recentBack", "recentForward", "quickTest"};

static void drawMenu() {
 if(focused!=-1) current = tabs.get(focused);
 String rest= " ".repeat(width);
 String tabNames= "";
 for (int i=0;i<tabs.size();i++){
  if(i==focused) {
   tabNames+= "!"+ tabs.get(i).fileName+ "!";
  }else{
   tabNames+= tabs.get(i).fileName;
  }
  tabNames= tabNames + " ";
 }

 StringBuilder combind= new StringBuilder();
 combind.append("<-").append(tabNames).append(rest);
 combind.insert(width-2,"->");
 System.out.print("\033[H"); // move to top
 System.out.print("\033[0m");     
 System.out.print("\033[48;2;14;107;55m");   // background 
 System.out.print("\033[38;2;255;255;255m"); // text 
 System.out.print( combind.substring(0,Math.min(width,combind.length())));
 System.out.print("\033[0m\n"); // reset
 System.out.flush();
}

public static int[] getTerminalSize() {
 int[] size = new int[2];
 try {
  Process p1 = Runtime.getRuntime().exec(new String[]{"bash", "-c", "tput cols < /dev/tty"});
  BufferedReader br1 = new BufferedReader(new InputStreamReader(p1.getInputStream()));
  String widthValue = br1.readLine();
  br1.close();
  size[0] = Integer.parseInt(widthValue.trim());

  Process p2 = Runtime.getRuntime().exec(new String[]{"bash", "-c", "tput lines < /dev/tty"});
  BufferedReader br2 = new BufferedReader(new InputStreamReader(p2.getInputStream()));
  String heightValue = br2.readLine();
  br2.close();
  size[1] = Integer.parseInt(heightValue.trim());
        
 } catch (Exception e) {
  size[0] = 82;  // fallback width
  size[1] = 24;  // fallback height
 }
 return size;
}

static void changeDirectory(String targetPathString) {
    Path currentDirPath = Paths.get(current.directory);
    Path targetPath = currentDirPath.resolve(targetPathString).normalize();
    if (Files.isDirectory(targetPath)) {
        current.directory = targetPath.toString();  // Convert back to String
    } else {
        System.out.println("Directory not found: " + targetPathString);
    }
}

private static void listFiles() {
 try (DirectoryStream<Path> stream = Files.newDirectoryStream(Paths.get(current.directory))) {
  for (Path entry : stream) {
   if (Files.isDirectory(entry)) {
    System.out.println("[DIR]  " + entry.getFileName());
   } else {
    System.out.println("[FILE] " + entry.getFileName());
   }
  }
 } catch (IOException e) {
  System.err.println("Error reading directory contents: " + e.getMessage());
 }
}

static void saveFile(String directory,String name){
 String result = current.lines.stream()
    .map(StringBuilder::toString)
    .collect(Collectors.joining()); Path filePath = Paths.get(directory, name);
 try (FileWriter writer = new FileWriter(filePath.toFile())) {
  writer.write(result);
 } catch (IOException e) {
  System.err.println("Error saving file: " + e.getMessage());
 }
}

static void updateCursor() {
 System.out.print("\033[" + (current.row + 2) + ";" + (current.column% width+ 1) + "H");
}

static void drawLoop(){
 if(current==null) return;
 System.out.print("\033[s"); 
 int amount=0; 
 Arrays.fill(displayLines,-1);

 for(int i=0; amount<height-1; i++){
  String text=" ";
  int index=0; int tabLength=1;
  if(i==0) index= current.remaining*width;
  if(current.lines.size()-1>=i + current.yScroll){
   tabLength= current.lines.get(i + current.yScroll).length();
  }
  
  if(!(amount<height-1 && index<=tabLength))
  {System.out.print("\033[" + (i+2) + ";1H\033[2K");}
  while(amount<height-1 && index<=tabLength){
   if(current.lines.size()-1>=i + current.yScroll){
    text= current.lines.get(i + current.yScroll).substring(index,Math.min(index+width, tabLength));
   }
   if (!text.equals(previousLines[amount])) {
    System.out.print("\033[" + (amount+2) + ";1H\033[2K");
    System.out.print("\033[" + (amount+2) + ";1H" + text);
    previousLines[amount] = text;
   }

   displayLines[amount+1]= i;
   amount++;
   index+=width;
  }
 }
 System.out.print("\033[u"); 
 System.out.flush();
}

static void openFile(String path, String name){
 tabs.add(new tab(name));
 focused= tabs.size()-1;
 tabs.get(focused).directory= path;
 try (Stream<String> lines = Files.lines(Paths.get(path+name), StandardCharsets.UTF_8)) {
  lines.forEach(line -> {current.lines.add(new StringBuilder(line));});
 } catch (Exception e) {e.printStackTrace();}
 drawMenu();
}

static void arrowKeys(int next2){
 if (next2 == 66) { // Down arrow
  if (current.lines.get(current.line).length()/width<= current.column/width){
   if (current.lines.size()-1<= current.line){
    current.column= current.lines.get(current.line).length();
   }else{
    current.row++; current.line++;
    current.column= Math.min(current.columnSave% width, current.lines.get(current.line).length());
    current.columnSave=current.columnSave% width;
   }
  } else{
   current.column= Math.min(current.lines.get(current.line).length(),current.columnSave+ width);
   current.columnSave+=width;
   current.row++;
  }
 } else if (next2 == 65) { // up arrow
  if (current.column/width<=0){
   if (current.line<=0){
    current.column= 0;
   }else{
    current.row--; current.line--;
    int keep= (current.lines.get(current.line).length()/width)*width+ current.columnSave;
    current.column= Math.min( current.lines.get(current.line).length(), keep); 
    current.columnSave= keep;
   }
  } else{
   current.column= current.columnSave- width;
   current.columnSave-=width;
   current.row--;
  }
 } else if (next2 == 67) { // right arrow
  if (current.column>= current.lines.get(current.line).length()){
   if (current.line+1<current.lines.size()){
    current.row++; current.line++; current.column= 0; 
    current.columnSave= 0;
   }
  }else{
   current.column++; current.columnSave++;
   if (current.column% width==0) current.row++;
  }
 } else if (next2 == 68) { // left arrow
  if (current.column<= 0){
   if (current.line>0){
    current.row--; current.line--;
    current.column= current.lines.get(current.line).length();
   }
  }else{
   if (current.column% width==0) current.row--;
   current.column--; current.columnSave--;
  }
 }
}

static void keyEvents() throws IOException{
 //if (System.in.available() == 0) return; 
 int ch = System.in.read();
 if (ch == -1) return;
 int change=current.row; int change2= current.line;

 if (ch == 127) { //backspace
  if (current.column<=0){
   if (current.row>0){
    current.row--;
    String rest= current.lines.get(current.line).toString();
    current.lines.remove(current.line);
    current.line--;
    current.column= current.lines.get(current.line).length();
    current.columnSave= current.column;
    current.lines.get(current.line).append(rest);
   }
  }else{
   if (current.column%width==0) current.row--;
   current.column--; current.columnSave= current.column;
   current.lines.get(current.line).deleteCharAt(current.column);
   if (current.lines.get(current.line).length()%width==0){
   }
  }
 }else if(ch==hotkeyByte[1]){
  saveFile(current.directory,current.fileName);
 }else if(ch==hotkeyByte[2]){
  //openFile(current.directory,current.fileName);
 } else if (ch == 10 || ch == 13) { // Enter
  String rest= current.lines.get(current.line).toString().substring(current.column, current.lines.get(current.line).length());
  String keep= "";
  if(current.column!=0) keep= current.lines.get(current.line).toString().substring(0,current.column );
  current.lines.set(current.line, new StringBuilder(keep));
  current.lines.add(current.line+1, new StringBuilder(rest));
  current.row++;
  current.line++;
  current.column = 0; current.columnSave= 0;
 } else if (ch == 27) { // ESC
  if (System.in.available() >= 2) {
   int next1 = System.in.read();
   int next2 = System.in.read();
   if (next1 == 91) { // '['
    arrowKeys(next2);
   }
   if (next2 == 51) { //del
    if (System.in.available() >= 1) {
     int next3 = System.in.read();
     if (next3 == 126) { //del
      if(current.column== current.lines.get(current.line).length()){
       if (current.lines.size()-1!= current.line){
        StringBuilder merged = new StringBuilder(current.lines.get(current.line));
        merged.append(current.lines.get(current.line + 1));
        current.lines.set(current.line, merged);
        current.lines.remove(current.line + 1);
       }
      }else{
       current.lines.get(current.line). deleteCharAt(current.column);
      }
     }
    } 
   }else if(next2 == 77){//mouseEvent
    if (System.in.available() >= 3) {
     int button = System.in.read();
     int x = System.in.read(); 
     int y = System.in.read();
     if (button == 0 || button == 32) {
      int mouseX = x-33;
      int mouseY = y-33;
      if(mouseY!=0&& !current.conEnabled){
       int amount=0;
       for(int i=mouseY-1;i>-1;i--) {
        if(displayLines[i]== displayLines[mouseY]) {
         amount++;
        }else break;
       }
       current.row=mouseY-1; current.line= displayLines[mouseY];
       int tabLength= current.lines.get(current.line).length();
       current.column= Math.min(tabLength,amount*width+ mouseX);
      }else if(mouseX< 5|| mouseX>width-5){
       if(mouseX<5){
        focused= Math.floorMod((focused-1), tabs.size());
       }else if(mouseX>width-5){
        focused= (focused+ 1)% tabs.size();
       }
       drawMenu();
      }else{
       current.conEnabled=!current.conEnabled;
       try {
        if (current.conEnabled) {
         new ProcessBuilder("sh", "-c", "stty -F /dev/tty cooked echo"). inheritIO().start().waitFor();
         new ProcessBuilder("clear"). inheritIO().start().waitFor();
         drawMenu();
        } else {
         new ProcessBuilder("sh", "-c", "stty -F /dev/tty raw -echo"). inheritIO().start().waitFor();
        }
       } catch (InterruptedException e) {
        Thread.currentThread().interrupt();
       }
      }
     }
    }
   }
  }
 } else {
  current.lines.get(current.line).insert(current.column, (char) ch);
  current.columnSave++; current.column++;
  if (current.column%width==0) current.row++;
 }

 double rowPosition = (double) current.row / height;
 if (current.row-change<0 || current.row-change>0){
  if (current.row-change<0){
   if (rowPosition<=0.25){
    if (current.yScroll>0){
     current.row++;
     if(current.remaining==0) {
      current.yScroll= current.yScroll-1;
      current.remaining= current.lines.get(current.yScroll).length()/width;
     }else{
      current.remaining--;
     }
    }
   }
  }else{
   if (rowPosition>=0.75){
    if (current.remaining>= current.lines.get(current.yScroll).length()/width){ 
     current.yScroll= Math.min( current.lines.size()-height/2,current.yScroll+1);
     current.remaining=0;
    }else{
     current.remaining++;
    }
    current.row--;
   }
  }
 }

 updateCursor();
 drawLoop();
}

public static void main(String[] args) throws Exception {
try {
 new ProcessBuilder("sh", "-c", "stty -F /dev/tty raw -echo").inheritIO().start().waitFor();
 while (System.in.available() > 0) System.in.read();
 new ProcessBuilder("clear").inheritIO().start().waitFor();
 drawLoop();

tabs.add(new tab("(save)"));
tabs.add(new tab("test"));
drawMenu();
System.out.print("\033[?1000h"); //mouseTracking
updateCursor();
tabs.get(0).lines.get(0).append("this is a new line");
tabs.get(0).lines.add(new StringBuilder("this is a bigggggggggggggggg gggggggg gggggg gggggg gggggg gggggg gggggg gggggg gggggg line"));
tabs.get(0).lines.add(new StringBuilder("this is a bigggggggggggggggg gggggggg gggggg gggggg gggggg gggggg gggggg gggggg ggggggggggg gggggg gggggg ggggggggggg gggggg gggggg ggggggggggg gggggg gggggg gggggg line"));
tabs.get(0).lines.add(new StringBuilder("this is the last line"));

for (int i = 0; i < 33; i++) {
 tabs.get(0).lines.add(new StringBuilder(String.valueOf(i)));
}
new Thread(() -> {
 while(true){
  try {
   tick++;
   if(tick>=10000000) tick=0;
   System.out.printf("\033]0;L:%d C:%d F:%s\007", current.line, current.column,current.fileName);
   int[] screenSize = getTerminalSize();
   if (tick%5==0 && width != screenSize[0] || height != screenSize[1]) {
    width = screenSize[0]; height = screenSize[1];
    previousLines= new String[height+10];
    displayLines= new int[height];
    drawLoop();
    drawMenu();
   }
   Thread.sleep(100); 
  } catch (InterruptedException e) {
   break;
  }
 }
}).start();

while (true) {
 keyEvents();
 Thread.sleep(10);
}
} finally {
 new ProcessBuilder("sh", "-c", "stty -F /dev/tty cooked echo").inheritIO().start().waitFor();
 }
 }
}

//!v4
import java.util.List;
import java.util.ArrayList;
import java.util.Arrays;
import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.IOException;

public class main{
 static class tab {
  List<String> lines = new ArrayList<>();
  int column = 0;
  int line= 0;
  int scrollX = 0; 
  int scrollY = 0;
  int saveColumn=0;
  String fileName="";
  String directory="";

  public tab(String fi) {
   lines.add("");
   this.fileName= fi; 
  }
 }

static int width = 80; static int height = 20;
static int focused = 0; static tab current;
static List<tab> tabs = new ArrayList<>();
static int tick=0; static int[] debug={0,0};
static int[] select= {-1,-1};
static int mx=-1; static int my=-1;
static String reset= "\u001B[0m";
static String reverse= "\u001B[7m";
static int[] hotkeyByte= {14,19, 15, 24, 23, 7, 4, 17, 5, 14, 15, 2};
String[] hotkeyName= {"new","save","open","cut", "close", "goto", "run", "leftTab", "rightTab", "recentBack", "recentForward", "quickTest"};
static int[] displayDir= {-1,-1,-1};
static int consoleHeight=-1;
static String[] screenBuffer = new String[10];

public static int[] getTerminalSize() {
 int[] size = new int[2];
 try {
  Process p1 = Runtime.getRuntime().exec(new String[]{"bash", "-c", "tput cols < /dev/tty"});
  BufferedReader br1 = new BufferedReader(new InputStreamReader(p1.getInputStream()));
  String widthValue = br1.readLine();
  br1.close();
  size[0] = Integer.parseInt(widthValue.trim());

  Process p2 = Runtime.getRuntime().exec(new String[]{"bash", "-c", "tput lines < /dev/tty"});
  BufferedReader br2 = new BufferedReader(new InputStreamReader(p2.getInputStream()));
  String heightValue = br2.readLine();
  br2.close();
  size[1] = Integer.parseInt(heightValue.trim());
        
 } catch (Exception e) {
  size[0] = 82;  // fallback width
  size[1] = 24;  // fallback height
 }
 return size;
}
   
static void handleMouse(int button){
 if(mx!=-1){
  current.line= my+ current.scrollY;
  current.column= mx+ current.scrollX;
 }
 current.line= Math.min(current.line, current.lines.size()-1);
 current.column= Math.min(current.column, current.lines.get(current.line). length());

 //32 64 35 click drag release
 if(button== 32){
  select[1]= current.line;
  select[0]= current.column;
 }else if(button==35){
  mx=-1; my=-1;
 }
}
 
static void inputEvents(int key) throws IOException{
 int next1=0; int next2=0; int next3=0;
 boolean redraw= true;

 if(select[0]!=-1){ 
  if(key!= 27&& (select[0]!= current.column|| select[1]!= current.line)){ //escKey
   int smallest= Math.min(current.line, select[1]);
   int highest= Math.max(current.line, select[1]);
   String remainder="";
   for(int i= highest; i>= smallest; i--){
    int convertI= i+ current.scrollY;
    String line= current.lines.get(convertI);
    int[] textI= extractSelect(convertI);
    current.lines.set(convertI, line.substring(0, textI[0]) + line.substring(textI[1]));
    line= current.lines.get(convertI);

    if(convertI== smallest&& convertI!= highest)
    {current.lines.set(convertI, line+ remainder);}
    else if(convertI== highest)
    {remainder= line;}
    if(convertI!= smallest)
    {current.lines.remove(convertI);}
   }
   current.column= Math.min(current.column, select[0]);
   current.line= smallest;
   select[0]=-1; select[1]=-1;
  }
 }
 if (key == 27) { // ESC character
  if (System.in.available() >= 1) {
   next1 = System.in.read();
   if (next1 == 91) { // '['
    if (System.in.available() >= 1) {
     next2 = System.in.read();
     if(next2==65) { // Up arrow
      current.line--;
     }else if(next2==66){ // Down arrow
      current.line++;
     }else if(next2==67){// right arrow
      current.column++;
     }else if(next2==68){ // Left arrow
      current.column--; 

     }else if(next2==77){//mouse
      if (System.in.available() >= 3) {
       int button= System.in.read(); 
       mx= System.in.read()- 33;
       my= System.in.read()- 33;
       handleMouse(button);
      }
     }else if (next2 == 51) { 
      if (System.in.available() >= 1) {
       if (System.in.read()== 126){ //del
        String line = current.lines.get(current.line);
        if (current.column!= line.length()) {
         current.lines.set(current.line, line.substring(0, current.column) + line.substring(current.column+1));
        } else if (current.line!= current.lines.size()-1) {
         current.lines.set(current.line, line+ current.lines.get(current.line+ 1));
         current.lines.remove( current.line+ 1);
   }}}}}}

   current.line= Math.max(0, Math.min(current.lines.size() - 1, current.line));
   current.column= Math.max(0, Math.min(current.lines.get(current.line).length(), current.column));
  }
 }else if (key == 17) { //ctrl+q
  System.exit(0);
 }else if (key == 13) { // Enter key
  current.lines.add(current.line + 1, current.lines.get(current.line).substring(current.column));
  current.lines.set(current.line, current.lines.get(current.line).substring(0, current.column));
  current.line++; current.column = 0;

 } else if (key == 127) { // Backspace
  if (current.column > 0) {
   String line = current.lines.get(current.line);
   current.lines.set(current.line, line.substring(0, current.column - 1) + line.substring(current.column));
   current.column--;
  } else if (current.line > 0) {
   current.column = current.lines.get(current.line - 1).length();
   current.lines.set(current.line - 1, current.lines.get(current.line - 1) + current.lines.get(current.line));
   current.lines.remove(current.line);
   current.line--;
  }

 } else if (key >= 32 && key <= 126) { //chars
  current.lines.set(current.line, current.lines.get(current.line).substring(0, current.column) + (char)key + current.lines.get(current.line).substring(current.column));
  current.column++;
 }

 if(redraw){
  scrollFunc();
  drawLines();
 }
}

static int[] extractSelect(int line){
 if(line<0|| line>current.lines.size()){
  return new int[] {0,0,0};
 }
 String currentStr= current.lines.get(line);
 int max= currentStr.length();
 if(select[0]==-1){
  return new int[] {max,max,max};
 }
 boolean selectBig= current.line> select[1]|| (current.line== select[1]&& current.column> select[0]);
 int startY = (!selectBig) ? current.line : select[1];
 int endY = (selectBig) ? current.line : select[1];
 int startX = (!selectBig) ? current.column : select[0];
 int endX = (selectBig) ? current.column : select[0];

 if(line> startY&& line< endY){
  return new int[] {0,max,max};
 }else if(line==startY&& line==endY){
  return new int[] {startX, endX,max};

 }else if(line==startY){
  return new int[] {startX, max,max};  
 }else if(line==endY){
  return new int[] {0, endX,max};   
 }

 return new int[] {max,max,max};
}

static void updateCursor() {
 System.out.print("\033[" + (current.line- current.scrollY + 1) + ";" + (current.column- current.scrollX + 1) + "H");
}

static void drawLines() {
 System.out.print("\033[?25l"); // cursor hide
 StringBuilder output = new StringBuilder();
 for (int i = 0; i < height; i++) {
  int convertI = i + current.scrollY;
  String lineContent= " ".repeat(width);
        
  if (convertI >= 0 && convertI < current.lines.size()) {
   String line = current.lines.get(convertI);
   int end = Math.min(current.scrollX + width, line.length());
   if (current.scrollX < end && current.scrollX <= line.length()) {
    int[] textI= extractSelect(convertI);
    for (int j = 0; j < textI.length; j++) 
    {textI[j] = Math.max(current.scrollX, Math.min(textI[j], current.scrollX + width));}
                
    String[] texts = {"", "", ""};
    if (line.length() > current.scrollX) {
     texts[0] = line.substring(current.scrollX, textI[0]);
     texts[1] = line.substring(textI[0], textI[1]);
     texts[2] = line.substring(textI[1], textI[2]);
    }
                
    lineContent = texts[0] + reverse + texts[1] + reset + texts[2];
   }
  }
  if (lineContent.length() < width)
  {lineContent= lineContent+ " ".repeat(width- lineContent.length()+ reverse.length()+ reset.length());}
        
  if (screenBuffer[i] == null || !screenBuffer[i].equals(lineContent)) {
   output.append("\033["). append(i + 1).append(";1H");
   output.append(lineContent);
   screenBuffer[i] = lineContent;
  }
 }
    
 if (output.length() > 0) 
 {System.out.print( output.toString());}
    
 updateCursor();
 System.out.print("\033[?25h"); //cursorshow
 System.out.flush();
}

static boolean scrollFunc(){
 double threshold= (mx==-1)? 0.15: 0.00;
 boolean active=false;

 if(height*threshold+1>= current.line- current.scrollY&& current.scrollY!=0){
  current.scrollY= Math.max(0, current.scrollY-1);
  active=true;
 }else if( height*(1.0-threshold)-1<= current.line- current.scrollY){
  current.scrollY= Math.min(current.lines.size(), current.scrollY+1);
  active=true;
 }
 if(current.column+2>=width+ current.scrollX){
  current.scrollX= Math.max(0, current.column- width+2);
  active=true;
 }
 if(current.column<= current.scrollX&& current.scrollX!=0){
  current.scrollX= Math.max(0,current.column-1);
  active=true;
 }
 return active;
}

 public static void main(String[] args)throws Exception {
  try {
 String[] cmd = {"/bin/sh", "-c", "stty -echo raw </dev/tty"};
 Runtime.getRuntime().exec(cmd) .waitFor();
 System.out.print("\033[?1002h");
 tabs.add(new tab("(save)"));
 current= tabs.get(focused);

new Thread(() -> {
 while(true){
  try{
   tick++;
   if(tick>=10000000) tick=0;
   System.out.printf("\033]0;L:%d C:%d %d %d %d\007", current.line, current.column, debug[0], debug[1], width);
   int[] screenSize = getTerminalSize();
   if(width!= screenSize[0]|| height!= screenSize[1]){
    width = screenSize[0]; height = screenSize[1];
    screenBuffer= Arrays.copyOf(screenBuffer, height);
   }
   if(select[1]!=-1) handleMouse(0);
   if(scrollFunc()) drawLines();
   Thread.sleep(100); 
  } catch (InterruptedException e) {
   break;
  }
 }
}).start();

 while(true){
  int key = System.in.read();
  inputEvents(key);
 }
  }finally{
   new ProcessBuilder("/bin/sh", "-c", "stty sane </dev/tty").start();
   System.out.print("\033[?1002l");
  }
 }//void
}//class




//!v5
import java.util.List;
import java.util.ArrayList;
import java.util.Arrays;
import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.IOException;

public class main{
 static class tab {
  List<String> lines = new ArrayList<>();
  int column = 0;
  int line= 0;
  int scrollX = 0; 
  int scrollY = 0;
  int saveColumn=0;
  String fileName="";
  String directory="";

  public tab(String fi) {
   lines.add("");
   this.fileName= fi; 
  }
 }

static int width = 80; static int height = 20;
static int focused = 0; static tab current;
static List<tab> tabs = new ArrayList<>();
static int tick=0; static int[] debug={0,0};
static int[] select= {-1,-1};
static String greenBackground = "\u001B[48;2;14;107;55m";
static String reset= "\u001B[0m";
static String reverse= "\u001B[7m";
static int[] hotkeyByte= {14,19, 15, 24, 23, 7, 4, 17, 5, 14, 15, 2};
String[] hotkeyName= {"new","save","open","cut", "close", "goto", "run", "leftTab", "rightTab", "recentBack", "recentForward", "quickTest"};
static int[] displayDir= {-1,-1,-1};
static int consoleHeight=1;
static String[] screenBuffer = new String[10];
static int[] readKeys= {0,0,0,0,0,0};
static int step=6;
static int mx=-1; static int my=-1;
static List<String> console= new ArrayList<>(Arrays.asList(""));
static int consoleState=0;// 0-textEditor 1-consoleDrag 2-console

public static int[] getTerminalSize() {
 int[] size = new int[2];
 try {
  Process p1 = Runtime.getRuntime().exec(new String[]{"bash", "-c", "tput cols < /dev/tty"});
  BufferedReader br1 = new BufferedReader(new InputStreamReader(p1.getInputStream()));
  String widthValue = br1.readLine();
  br1.close();
  size[0] = Integer.parseInt(widthValue.trim());

  Process p2 = Runtime.getRuntime().exec(new String[]{"bash", "-c", "tput lines < /dev/tty"});
  BufferedReader br2 = new BufferedReader(new InputStreamReader(p2.getInputStream()));
  String heightValue = br2.readLine();
  br2.close();
  size[1] = Integer.parseInt(heightValue.trim());
        
 } catch (Exception e) {
  size[0] = 82;  // fallback width
  size[1] = 24;  // fallback height
 }
 return size;
}
   
static void handleMouse(int button){
 if(mx!=-1&& consoleState==0){
  current.line= readKeys[5]+ current.scrollY;
  current.column= readKeys[4]+ current.scrollX;
  current.line= Math.max(0, Math.min(current.line, current.lines.size()-1));
  current.column= Math.min(current.column, current.lines.get(current.line). length());
  current.saveColumn= current.column;
 }

 //32 64 35 click drag release
 if(button==35&& readKeys[5]>= height-consoleHeight){
  consoleState=2;
 }else if(button== 35&& consoleState==0){
  mx=-1; my=-1;
  if(select[0]== current.column&& select[1]== current.line){
   select[0]=-1; select[1]=-1;
  }
 }else if(button== 32&& readKeys[5]< height-consoleHeight){
  consoleState=0;
  select[1]= current.line;
  select[0]= current.column;
 }else if(button==32){
  consoleState=1;
 }else if(button== 64&& consoleState==1){
  consoleHeight= height- readKeys[5];
 }
}

static boolean deleteSelect(){
 if(select[0]==-1&& (current.line!= select[1]|| current.column!=select[0])){
  select[0]=-1; select[1]=-1;
  return false;
 }
 int smallest= Math.min(current.line, select[1]);
 int highest= Math.max(current.line, select[1]);
 String remainder="";
 for(int i= highest; i>= smallest; i--){
  int convertI= i+ current.scrollY;
  String line= current.lines.get(convertI);
  int[] textI= extractSelect(convertI);
  current.lines.set(convertI, line.substring(0, textI[0]) + line.substring(textI[1]));
  line= current.lines.get(convertI);

  if(convertI== smallest&& convertI!= highest)
  {current.lines.set(convertI, line+ remainder);}
  else if(convertI== highest)
  {remainder= line;}
  if(convertI!= smallest)
  {current.lines.remove(convertI);}
 }
 current.column= Math.min(current.column, select[0]);
 current.line= smallest;
 select[0]=-1; select[1]=-1;
 return true;
}

static void escapeKeys(){
 boolean upDownArrow= false;
 boolean leftRightArrow= false;
 int newStep= 1;
 if (readKeys[0] == 91) { // '['
  if(readKeys[3]==53){
   newStep= step;
  }
  if(readKeys[1]>=65&& readKeys[1]<=68){
   select[0]=-1; select[1]=-1;
  }else if(readKeys[4]>=65&& readKeys[4]<=68&& readKeys[1]==49&& readKeys[3]== 50){
   if(select[0]==-1){
    select[0]= current.column;
    select[1]= current.line;
   }
  }
  if(readKeys[1]==65|| (readKeys[1]==49&& readKeys[4]==65)) { // Up arrow
   current.line= current.line- newStep;
   current.column= current.saveColumn;
   upDownArrow=true;
  }else if(readKeys[1]==66|| (readKeys[1]==49&& readKeys[4]==66)){ // Down arrow
   current.line= current.line+ newStep;
   current.column= current.saveColumn;
   upDownArrow=true;
  }else if(readKeys[1]==67|| (readKeys[1]==49&& readKeys[4]==67)){// right arrow
   current.column= current.column+ newStep;
   leftRightArrow= true;
  }else if(readKeys[1]==68|| (readKeys[1]==49&& readKeys[4]==68)){ // Left arrow
   current.column= current.column- newStep; 
   leftRightArrow= true;
  }else if (readKeys[1] == 51) { 
   if (readKeys[2]== 126){ //del
    String line = current.lines.get(current.line);
    if(deleteSelect()){
    }else if (current.column!= line.length()) {
     current.lines.set(current.line, line.substring(0, current.column) + line.substring(current.column+1));
    } else if (current.line!= current.lines.size()-1) {
     current.lines.set(current.line, line+ current.lines.get(current.line+ 1));
     current.lines.remove( current.line+ 1);
 }}}}

 if(current.line< current.lines.size()&& current.line>-1&& !upDownArrow){
  if(current.column> current.lines.get(current.line).length()&& current.line< current.lines.size()-1) {
   current.line++; current.column= 0;
  }else if(current.column< 0&& current.line>0){
   current.line--;
   current.column= current.lines.get(current.line).length();
  }
 }
 current.line= Math.max(0, Math.min(current.lines.size() - 1, current.line));
 current.column= Math.max(0, Math.min(current.lines.get(current.line).length(), current.column));
 if(leftRightArrow)
 {current.saveColumn= current.column;}
}
 
static void inputEvents(int key) throws IOException{
 for(int i= 0; i<readKeys.length;i++)
 {readKeys[i]=-1;}
 boolean redraw= true;

 if (key == 27) { // ESC character
 for(int i= 0; i<readKeys.length;i++){
  }
  if (System.in.available() > 0) {
   readKeys[0] = System.in.read();
   if (System.in.available() > 0) {
    readKeys[1] = System.in.read();
    if(readKeys[1]==77){//mouse
     if (System.in.available() >= 3) {
      readKeys[3]= System.in.read(); //button 
      readKeys[4]= System.in.read()- 33; //x
      readKeys[5]= System.in.read()- 33;// y
      mx=readKeys[4]; my= readKeys[5];
      handleMouse(readKeys[3]);
     }
    }
    if (System.in.available() > 0) {
     readKeys[2] = System.in.read();
     if(readKeys[2]== 59){ //;
      readKeys[3] = System.in.read();// modifier
      readKeys[4] = System.in.read();// actionChar
     }
    }
    escapeKeys();
   }
  }
 }else if (key == 17) { //ctrl+q
  System.exit(0);
 }else if (key == 13) { // Enter key
  deleteSelect();
  current.lines.add(current.line + 1, current.lines.get(current.line).substring(current.column));
  current.lines.set(current.line, current.lines.get(current.line).substring(0, current.column));
  current.line++; current.column = 0;

 } else if (key == 127) { // Backspace
  if(deleteSelect()){
  }else if (current.column > 0) {
   String line = current.lines.get(current.line);
   current.lines.set(current.line, line.substring(0, current.column - 1) + line.substring(current.column));
   current.column--;
  } else if (current.line > 0) {
   current.column = current.lines.get(current.line - 1).length();
   current.lines.set(current.line - 1, current.lines.get(current.line - 1) + current.lines.get(current.line));
   current.lines.remove(current.line);
   current.line--;
   current.saveColumn= current.column;
  }
 }

 if (key >= 32 && key <= 126) { //chars
  deleteSelect();
  current.lines.set(current.line, current.lines.get(current.line).substring(0, current.column) + (char)key + current.lines.get(current.line).substring(current.column));
  current.column++;
  current.saveColumn= current.column;
 }else if(key==hotkeyByte[0]){
  
 }

 if(redraw){
  scrollFunc();
  drawLines();
 }
}

static int[] extractSelect(int line){
 if(line<0|| line>current.lines.size()){
  return new int[] {0,0,0};
 }
 String currentStr= current.lines.get(line);
 int max= currentStr.length();
 if(select[0]==-1){
  return new int[] {max,max,max};
 }
 boolean selectBig= current.line> select[1]|| (current.line== select[1]&& current.column> select[0]);
 int startY = (!selectBig) ? current.line : select[1];
 int endY = (selectBig) ? current.line : select[1];
 int startX = (!selectBig) ? current.column : select[0];
 int endX = (selectBig) ? current.column : select[0];

 if(line> startY&& line< endY){
  return new int[] {0,max,max};
 }else if(line==startY&& line==endY){
  return new int[] {startX, endX,max};

 }else if(line==startY){
  return new int[] {startX, max,max};  
 }else if(line==endY){
  return new int[] {0, endX,max};   
 }

 return new int[] {max,max,max};
}

static void updateCursor() {
 System.out.print("\033[" + (current.line- current.scrollY + 1) + ";" + (current.column- current.scrollX + 1) + "H");
}

static void drawLines() {
 System.out.print("\033[?25l"); // cursor hide
 StringBuilder output = new StringBuilder();
 for (int i = 0; i < screenBuffer.length; i++) {
  int convertI = i + current.scrollY;
  String lineContent= " ".repeat(width);
  int displayLength=0;
        
  if (convertI >= 0 && convertI < current.lines.size()&& i<height- consoleHeight) {
   String fullLine= current.lines.get(convertI);
   if( fullLine.length()>=current.scrollX){
    char startChar='\0';
    char endChar='\0';
    String partLine= fullLine.substring(current.scrollX, Math.min(fullLine.length(), current.scrollX+ width));
    displayLength= partLine.length();
    if(fullLine.length()>= current.scrollX + width){
     endChar= fullLine.charAt(current.scrollX + width - 1);
     partLine= partLine.substring(0,width-1);
    }
    if(fullLine.length()>0&& current.scrollX!= 0){
     if(partLine.length()>0){
      startChar= partLine.charAt(0);
      partLine= partLine.substring(1);
     }else{
      startChar=' ';
     }
    }
    int[] textI= extractSelect(convertI);
    for(int j=0;j<3;j++)
    {textI[j]= Math.min(width, Math.min(textI[j]- current.scrollX, partLine.length()));}
    String[] texts = {"", "", ""};
    texts[0] = partLine.substring(0, textI[0]);
    texts[1] = partLine.substring(textI[0], textI[1]);
    texts[2] = partLine.substring(textI[1], textI[2]);
                  
    lineContent = greenBackground+ startChar+ reset+ texts[0] + reverse + texts[1] + reset + texts[2]+ greenBackground+ endChar+ reset;
   }else{ //fullLine<scrollX
    if(current.lines. get(convertI).length()> 0){
     displayLength=1;
     lineContent= greenBackground+ ' '+ reset;
    }
   }
  }else if(i>=height-consoleHeight){ 
   if(i==height- consoleHeight)
   {lineContent= greenBackground;}
   lineContent+= " ".repeat(width);
   if(i==height-1)
   {lineContent+= reset;}
   displayLength= width;
  }

  if (displayLength < width)
  {lineContent= lineContent+ " ".repeat( width- displayLength);}

  if ((screenBuffer[i] == null || !screenBuffer[i].equals(lineContent))) {
   output.append("\033[").append(i + 1).append(";1H");
   output.append(lineContent);
   screenBuffer[i] = lineContent;
  }
 } 
 if (output.length() > 0) {
  System.out.print(output.toString());
  debug[0]= output.toString().length();
 }
    
 updateCursor();
 System.out.print("\033[?25h"); //cursorshow
 System.out.flush();
}

static boolean scrollFunc(){
 double threshold= (readKeys[4]==-1)? 0.15: 0.00;
 boolean active=false;

 if(height*threshold+1>= current.line- current.scrollY&& current.scrollY!=0){
  current.scrollY= Math.max(0, current.scrollY-1);
  active=true;
 }else if( height*(1.0-threshold)-1<= current.line- current.scrollY){
  current.scrollY= Math.min(current.lines.size(), current.scrollY+1);
  active=true;
 }
 if(current.column+1>=width+ current.scrollX){
  current.scrollX= Math.max(0, current.column- width+1);
  active=true;
 }
 if(current.column<= current.scrollX&& current.scrollX!=0){
  current.scrollX= Math.max(0,current.column-1);
  active=true;
 }
 return active;
}

 public static void main(String[] args)throws Exception {
  try {
 String[] cmd = {"/bin/sh", "-c", "clear;  stty -echo raw </dev/tty"};
 Runtime.getRuntime().exec(cmd) .waitFor();
 System.out.print("\033[?1002h");
 tabs.add(new tab("(save)"));
 current= tabs.get(focused);

 int[] screenSize1 = getTerminalSize();
 width = screenSize1[0];
 height = screenSize1[1];
 screenBuffer = new String[height];
 drawLines();

new Thread(() -> {
 while(true){
  try{
   tick++;
   if(tick>=10000000) tick=0;
   System.out.printf("\033]0;L:%d C:%d %d %d %d %d\007", current.line, current.column,debug[0], readKeys[3], select[0], consoleHeight);
   int[] screenSize = getTerminalSize();
   if(width!= screenSize[0]|| height!= screenSize[1]){
    width = screenSize[0]; height = screenSize[1];
    screenBuffer= Arrays.copyOf(screenBuffer, height);
   }
   if(select[1]!=-1) handleMouse(-1);
   if(scrollFunc()) drawLines();
   Thread.sleep(100); 
  } catch (InterruptedException e) {
   break;
  }
 }
}).start();

 while(true){
  int key = System.in.read();
  inputEvents(key);
 }
  }finally{
   new ProcessBuilder("/bin/sh", "-c", "stty sane </dev/tty").start();
   System.out.print("\033[?1002l");
  }
 }//void
}//class